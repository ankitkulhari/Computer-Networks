#include<stdio.h>
#include<stdlib.h>
#include<unistd.h>
char* convertToBinary(int k){
	
}
int main(int argn,char* arg[]){
	printf("Give Input code in Hex\n");
	char code[1000];
	scanf(" %s",code);
	int n=strlen(code)*4;
	int arr[n+100];
	memset(arr,0,sizeof(arr));
	

}